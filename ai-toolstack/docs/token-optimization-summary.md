# Token Optimization — Agent Summary

Compact index. Detail: [`token-optimization-overview.md`](token-optimization-overview.md).

## Law

No cloud exfiltration without explicit consent (`no-cloud-exfiltration.mdc`).

## Ownership

Persian typography + source English → **user global**. Project rules → cloud / root-cause / hub / ponytail / repo-docs English / long jobs (+ scoped globs). Do not restate global text in project rules.

## Tools

Docs+rg · mcp-lazy · Memory (chat facts) · Headroom (large blobs) · RTK (shell) · Ponytail (minimum code / terse chat).

## Workflow

1. Read `backend/docs/standards/<service>/`
2. Narrow `rg` → callers before backend edits
3. Headroom on large tool output when needed
4. Live/smoke when asked — `live-feature-qa`

## MCP

Cursor `mcp.json`: **mcp-lazy** only. Backends in `mcp-lazy-servers.json`: memory + headroom.
