# PostgreSQL is the only runtime persistence backend.
AGENTCORE_RULE_ENGINE_DATABASE_URL=postgresql://agentcore:change-me@postgres:5432/agentcore
# Judge adapter: heuristic (default, tests/bootstrap) or litellm (GAP-T05).
AGENTCORE_RULE_JUDGE=heuristic
